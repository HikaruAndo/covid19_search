class CovidData {
  CovidData({this.name, this.population, this.cases, this.hospitalize, this.severe, this.deaths});

  // 地名
  String name;
  // 総人口
  int population;
  // 陽性者
  int cases;
  // 入院者
  int hospitalize;
  // 重傷者
  int severe;
  // 死者
  int deaths;
}